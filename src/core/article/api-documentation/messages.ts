import { DefaultMessage } from 'src/common/decorators'

export const ArticleMessage = {
  CREATED: DefaultMessage.CREATED('Article'),
  UPDATED: DefaultMessage.UPDATED('Article'),
  DELETED: DefaultMessage.DELETED('Article'),
}
